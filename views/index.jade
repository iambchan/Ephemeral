extend template
block content
  h4 Send a message to yourself, a friend, or loved one in the future and we'll send you the message in the specified time - whether it be a month or year.
  hr
  form(action='/message', method='POST')
    div(class='form-group')
      label(for='recipient') Recipient Email Address
      input(type='email', class='form-control', name='recipient', placeholder='Enter Email' required)
    div(class='form-group')
      label(for='content') Message
      textarea(class='form-control', name='content', rows='3' required)
    div(class='form-group')
      label(for='send_date') Send Date
      br
      input(type='date', name='send_date' id='send_date')
      button(onclick='return setTodaysDate()') Today
      button(onclick='return setAMonthDate()') A Month
    div(class='form-group')
      label(for='from_user') From
      br
      input(type='text', name='from_user')
    button(type='submit',class='btn btn-default') Submit
